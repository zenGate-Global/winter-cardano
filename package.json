{
	"name": "lib-example-bun",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"build": "bun bun.config.ts",
		"test": "vitest",
		"lint": "biome lint",
		"format": "biome format",
		"check": "biome check",
		"clean": "rm -rf ./dist && echo 'Done: Library output dist folder removed.'",
		"reset": "rm -rf ./node_modules && echo 'Done: Node modules folder removed.'"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/bun": "latest",
		"dotenv": "^16.4.7",
		"vitest": "^3.1.1"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@meshsdk/core": "1.8.14"
	}
}
